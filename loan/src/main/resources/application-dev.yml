server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://rsbankdb:3306/rsbank?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
      
logging:
  level:
    org:
      hibernate:
        sql: DEBUG
